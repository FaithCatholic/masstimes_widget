name: MassTimes Widget (DDEV)
type: module
description: 'DDEV-only companion for MassTimes Widget. Provides whatever the local development environment needs to exercise the widget. Never enable this outside local development.'
package: Development
core_version_requirement: ^10.2 || ^11
dependencies:
  - drupal:block
  - masstimes_widget
